/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    fixDecoderSize,
    fixEncoderSize,
    getBytesDecoder,
    getBytesEncoder,
    getStructDecoder,
    getStructEncoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlySignerAccount,
    type ReadonlyUint8Array,
    type TransactionSigner,
    type WritableAccount,
} from '@solana/kit'
import {
    getAccountMetaFactory,
    type ResolvedInstructionAccount,
} from '@solana/program-client-core'
import { DYNAMIC_BONDING_CURVE_PROGRAM_ADDRESS } from '../programs'

export const CLOSE_OPERATOR_ACCOUNT_DISCRIMINATOR = new Uint8Array([
    171, 9, 213, 74, 120, 23, 3, 29,
])

export function getCloseOperatorAccountDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(
        CLOSE_OPERATOR_ACCOUNT_DISCRIMINATOR
    )
}

export type CloseOperatorAccountInstruction<
    TProgram extends string = typeof DYNAMIC_BONDING_CURVE_PROGRAM_ADDRESS,
    TAccountOperator extends string | AccountMeta<string> = string,
    TAccountSigner extends string | AccountMeta<string> = string,
    TAccountRentReceiver extends string | AccountMeta<string> = string,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountOperator extends string
                ? WritableAccount<TAccountOperator>
                : TAccountOperator,
            TAccountSigner extends string
                ? ReadonlySignerAccount<TAccountSigner> &
                      AccountSignerMeta<TAccountSigner>
                : TAccountSigner,
            TAccountRentReceiver extends string
                ? WritableAccount<TAccountRentReceiver>
                : TAccountRentReceiver,
            ...TRemainingAccounts,
        ]
    >

export type CloseOperatorAccountInstructionData = {
    discriminator: ReadonlyUint8Array
}

export type CloseOperatorAccountInstructionDataArgs = {}

export function getCloseOperatorAccountInstructionDataEncoder(): FixedSizeEncoder<CloseOperatorAccountInstructionDataArgs> {
    return transformEncoder(
        getStructEncoder([
            ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
        ]),
        (value) => ({
            ...value,
            discriminator: CLOSE_OPERATOR_ACCOUNT_DISCRIMINATOR,
        })
    )
}

export function getCloseOperatorAccountInstructionDataDecoder(): FixedSizeDecoder<CloseOperatorAccountInstructionData> {
    return getStructDecoder([
        ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ])
}

export function getCloseOperatorAccountInstructionDataCodec(): FixedSizeCodec<
    CloseOperatorAccountInstructionDataArgs,
    CloseOperatorAccountInstructionData
> {
    return combineCodec(
        getCloseOperatorAccountInstructionDataEncoder(),
        getCloseOperatorAccountInstructionDataDecoder()
    )
}

export type CloseOperatorAccountInput<
    TAccountOperator extends string = string,
    TAccountSigner extends string = string,
    TAccountRentReceiver extends string = string,
> = {
    operator: Address<TAccountOperator>
    signer: TransactionSigner<TAccountSigner>
    rentReceiver: Address<TAccountRentReceiver>
}

export function getCloseOperatorAccountInstruction<
    TAccountOperator extends string,
    TAccountSigner extends string,
    TAccountRentReceiver extends string,
    TProgramAddress extends Address =
        typeof DYNAMIC_BONDING_CURVE_PROGRAM_ADDRESS,
>(
    input: CloseOperatorAccountInput<
        TAccountOperator,
        TAccountSigner,
        TAccountRentReceiver
    >,
    config?: { programAddress?: TProgramAddress }
): CloseOperatorAccountInstruction<
    TProgramAddress,
    TAccountOperator,
    TAccountSigner,
    TAccountRentReceiver
> {
    // Program address.
    const programAddress =
        config?.programAddress ?? DYNAMIC_BONDING_CURVE_PROGRAM_ADDRESS

    // Original accounts.
    const originalAccounts = {
        operator: { value: input.operator ?? null, isWritable: true },
        signer: { value: input.signer ?? null, isWritable: false },
        rentReceiver: { value: input.rentReceiver ?? null, isWritable: true },
    }
    const accounts = originalAccounts as Record<
        keyof typeof originalAccounts,
        ResolvedInstructionAccount
    >

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
    return Object.freeze({
        accounts: [
            getAccountMeta('operator', accounts.operator),
            getAccountMeta('signer', accounts.signer),
            getAccountMeta('rentReceiver', accounts.rentReceiver),
        ],
        data: getCloseOperatorAccountInstructionDataEncoder().encode({}),
        programAddress,
    } as CloseOperatorAccountInstruction<
        TProgramAddress,
        TAccountOperator,
        TAccountSigner,
        TAccountRentReceiver
    >)
}

export type ParsedCloseOperatorAccountInstruction<
    TProgram extends string = typeof DYNAMIC_BONDING_CURVE_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>
    accounts: {
        operator: TAccountMetas[0]
        signer: TAccountMetas[1]
        rentReceiver: TAccountMetas[2]
    }
    data: CloseOperatorAccountInstructionData
}

export function parseCloseOperatorAccountInstruction<
    TProgram extends string,
    TAccountMetas extends readonly AccountMeta[],
>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>
): ParsedCloseOperatorAccountInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 3) {
        throw new SolanaError(
            SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
            {
                actualAccountMetas: instruction.accounts.length,
                expectedAccountMetas: 3,
            }
        )
    }
    let accountIndex = 0
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[
            accountIndex
        ]!
        accountIndex += 1
        return accountMeta
    }
    return {
        programAddress: instruction.programAddress,
        accounts: {
            operator: getNextAccount(),
            signer: getNextAccount(),
            rentReceiver: getNextAccount(),
        },
        data: getCloseOperatorAccountInstructionDataDecoder().decode(
            instruction.data
        ),
    }
}
