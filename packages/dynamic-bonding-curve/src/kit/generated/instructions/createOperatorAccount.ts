/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    fixDecoderSize,
    fixEncoderSize,
    getBytesDecoder,
    getBytesEncoder,
    getStructDecoder,
    getStructEncoder,
    getU128Decoder,
    getU128Encoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlyAccount,
    type ReadonlySignerAccount,
    type ReadonlyUint8Array,
    type TransactionSigner,
    type WritableAccount,
    type WritableSignerAccount,
} from '@solana/kit'
import {
    getAccountMetaFactory,
    getAddressFromResolvedInstructionAccount,
    type ResolvedInstructionAccount,
} from '@solana/program-client-core'
import { findEventAuthorityPda, findOperatorPda } from '../pdas'
import { DYNAMIC_BONDING_CURVE_PROGRAM_ADDRESS } from '../programs'

export const CREATE_OPERATOR_ACCOUNT_DISCRIMINATOR = new Uint8Array([
    221, 64, 246, 149, 240, 153, 229, 163,
])

export function getCreateOperatorAccountDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(
        CREATE_OPERATOR_ACCOUNT_DISCRIMINATOR
    )
}

export type CreateOperatorAccountInstruction<
    TProgram extends string = typeof DYNAMIC_BONDING_CURVE_PROGRAM_ADDRESS,
    TAccountOperator extends string | AccountMeta<string> = string,
    TAccountWhitelistedAddress extends string | AccountMeta<string> = string,
    TAccountSigner extends string | AccountMeta<string> = string,
    TAccountPayer extends string | AccountMeta<string> = string,
    TAccountSystemProgram extends string | AccountMeta<string> =
        '11111111111111111111111111111111',
    TAccountEventAuthority extends string | AccountMeta<string> = string,
    TAccountProgram extends string | AccountMeta<string> = string,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountOperator extends string
                ? WritableAccount<TAccountOperator>
                : TAccountOperator,
            TAccountWhitelistedAddress extends string
                ? ReadonlyAccount<TAccountWhitelistedAddress>
                : TAccountWhitelistedAddress,
            TAccountSigner extends string
                ? ReadonlySignerAccount<TAccountSigner> &
                      AccountSignerMeta<TAccountSigner>
                : TAccountSigner,
            TAccountPayer extends string
                ? WritableSignerAccount<TAccountPayer> &
                      AccountSignerMeta<TAccountPayer>
                : TAccountPayer,
            TAccountSystemProgram extends string
                ? ReadonlyAccount<TAccountSystemProgram>
                : TAccountSystemProgram,
            TAccountEventAuthority extends string
                ? ReadonlyAccount<TAccountEventAuthority>
                : TAccountEventAuthority,
            TAccountProgram extends string
                ? ReadonlyAccount<TAccountProgram>
                : TAccountProgram,
            ...TRemainingAccounts,
        ]
    >

export type CreateOperatorAccountInstructionData = {
    discriminator: ReadonlyUint8Array
    permission: bigint
}

export type CreateOperatorAccountInstructionDataArgs = {
    permission: number | bigint
}

export function getCreateOperatorAccountInstructionDataEncoder(): FixedSizeEncoder<CreateOperatorAccountInstructionDataArgs> {
    return transformEncoder(
        getStructEncoder([
            ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
            ['permission', getU128Encoder()],
        ]),
        (value) => ({
            ...value,
            discriminator: CREATE_OPERATOR_ACCOUNT_DISCRIMINATOR,
        })
    )
}

export function getCreateOperatorAccountInstructionDataDecoder(): FixedSizeDecoder<CreateOperatorAccountInstructionData> {
    return getStructDecoder([
        ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
        ['permission', getU128Decoder()],
    ])
}

export function getCreateOperatorAccountInstructionDataCodec(): FixedSizeCodec<
    CreateOperatorAccountInstructionDataArgs,
    CreateOperatorAccountInstructionData
> {
    return combineCodec(
        getCreateOperatorAccountInstructionDataEncoder(),
        getCreateOperatorAccountInstructionDataDecoder()
    )
}

export type CreateOperatorAccountAsyncInput<
    TAccountOperator extends string = string,
    TAccountWhitelistedAddress extends string = string,
    TAccountSigner extends string = string,
    TAccountPayer extends string = string,
    TAccountSystemProgram extends string = string,
    TAccountEventAuthority extends string = string,
    TAccountProgram extends string = string,
> = {
    operator?: Address<TAccountOperator>
    whitelistedAddress: Address<TAccountWhitelistedAddress>
    signer: TransactionSigner<TAccountSigner>
    payer: TransactionSigner<TAccountPayer>
    systemProgram?: Address<TAccountSystemProgram>
    eventAuthority?: Address<TAccountEventAuthority>
    program: Address<TAccountProgram>
    permission: CreateOperatorAccountInstructionDataArgs['permission']
}

export async function getCreateOperatorAccountInstructionAsync<
    TAccountOperator extends string,
    TAccountWhitelistedAddress extends string,
    TAccountSigner extends string,
    TAccountPayer extends string,
    TAccountSystemProgram extends string,
    TAccountEventAuthority extends string,
    TAccountProgram extends string,
    TProgramAddress extends Address =
        typeof DYNAMIC_BONDING_CURVE_PROGRAM_ADDRESS,
>(
    input: CreateOperatorAccountAsyncInput<
        TAccountOperator,
        TAccountWhitelistedAddress,
        TAccountSigner,
        TAccountPayer,
        TAccountSystemProgram,
        TAccountEventAuthority,
        TAccountProgram
    >,
    config?: { programAddress?: TProgramAddress }
): Promise<
    CreateOperatorAccountInstruction<
        TProgramAddress,
        TAccountOperator,
        TAccountWhitelistedAddress,
        TAccountSigner,
        TAccountPayer,
        TAccountSystemProgram,
        TAccountEventAuthority,
        TAccountProgram
    >
> {
    // Program address.
    const programAddress =
        config?.programAddress ?? DYNAMIC_BONDING_CURVE_PROGRAM_ADDRESS

    // Original accounts.
    const originalAccounts = {
        operator: { value: input.operator ?? null, isWritable: true },
        whitelistedAddress: {
            value: input.whitelistedAddress ?? null,
            isWritable: false,
        },
        signer: { value: input.signer ?? null, isWritable: false },
        payer: { value: input.payer ?? null, isWritable: true },
        systemProgram: {
            value: input.systemProgram ?? null,
            isWritable: false,
        },
        eventAuthority: {
            value: input.eventAuthority ?? null,
            isWritable: false,
        },
        program: { value: input.program ?? null, isWritable: false },
    }
    const accounts = originalAccounts as Record<
        keyof typeof originalAccounts,
        ResolvedInstructionAccount
    >

    // Original args.
    const args = { ...input }

    // Resolve default values.
    if (!accounts.operator.value) {
        accounts.operator.value = await findOperatorPda({
            whitelistedAddress: getAddressFromResolvedInstructionAccount(
                'whitelistedAddress',
                accounts.whitelistedAddress.value
            ),
        })
    }
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
    }
    if (!accounts.eventAuthority.value) {
        accounts.eventAuthority.value = await findEventAuthorityPda()
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
    return Object.freeze({
        accounts: [
            getAccountMeta('operator', accounts.operator),
            getAccountMeta('whitelistedAddress', accounts.whitelistedAddress),
            getAccountMeta('signer', accounts.signer),
            getAccountMeta('payer', accounts.payer),
            getAccountMeta('systemProgram', accounts.systemProgram),
            getAccountMeta('eventAuthority', accounts.eventAuthority),
            getAccountMeta('program', accounts.program),
        ],
        data: getCreateOperatorAccountInstructionDataEncoder().encode(
            args as CreateOperatorAccountInstructionDataArgs
        ),
        programAddress,
    } as CreateOperatorAccountInstruction<
        TProgramAddress,
        TAccountOperator,
        TAccountWhitelistedAddress,
        TAccountSigner,
        TAccountPayer,
        TAccountSystemProgram,
        TAccountEventAuthority,
        TAccountProgram
    >)
}

export type CreateOperatorAccountInput<
    TAccountOperator extends string = string,
    TAccountWhitelistedAddress extends string = string,
    TAccountSigner extends string = string,
    TAccountPayer extends string = string,
    TAccountSystemProgram extends string = string,
    TAccountEventAuthority extends string = string,
    TAccountProgram extends string = string,
> = {
    operator: Address<TAccountOperator>
    whitelistedAddress: Address<TAccountWhitelistedAddress>
    signer: TransactionSigner<TAccountSigner>
    payer: TransactionSigner<TAccountPayer>
    systemProgram?: Address<TAccountSystemProgram>
    eventAuthority: Address<TAccountEventAuthority>
    program: Address<TAccountProgram>
    permission: CreateOperatorAccountInstructionDataArgs['permission']
}

export function getCreateOperatorAccountInstruction<
    TAccountOperator extends string,
    TAccountWhitelistedAddress extends string,
    TAccountSigner extends string,
    TAccountPayer extends string,
    TAccountSystemProgram extends string,
    TAccountEventAuthority extends string,
    TAccountProgram extends string,
    TProgramAddress extends Address =
        typeof DYNAMIC_BONDING_CURVE_PROGRAM_ADDRESS,
>(
    input: CreateOperatorAccountInput<
        TAccountOperator,
        TAccountWhitelistedAddress,
        TAccountSigner,
        TAccountPayer,
        TAccountSystemProgram,
        TAccountEventAuthority,
        TAccountProgram
    >,
    config?: { programAddress?: TProgramAddress }
): CreateOperatorAccountInstruction<
    TProgramAddress,
    TAccountOperator,
    TAccountWhitelistedAddress,
    TAccountSigner,
    TAccountPayer,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
> {
    // Program address.
    const programAddress =
        config?.programAddress ?? DYNAMIC_BONDING_CURVE_PROGRAM_ADDRESS

    // Original accounts.
    const originalAccounts = {
        operator: { value: input.operator ?? null, isWritable: true },
        whitelistedAddress: {
            value: input.whitelistedAddress ?? null,
            isWritable: false,
        },
        signer: { value: input.signer ?? null, isWritable: false },
        payer: { value: input.payer ?? null, isWritable: true },
        systemProgram: {
            value: input.systemProgram ?? null,
            isWritable: false,
        },
        eventAuthority: {
            value: input.eventAuthority ?? null,
            isWritable: false,
        },
        program: { value: input.program ?? null, isWritable: false },
    }
    const accounts = originalAccounts as Record<
        keyof typeof originalAccounts,
        ResolvedInstructionAccount
    >

    // Original args.
    const args = { ...input }

    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
    return Object.freeze({
        accounts: [
            getAccountMeta('operator', accounts.operator),
            getAccountMeta('whitelistedAddress', accounts.whitelistedAddress),
            getAccountMeta('signer', accounts.signer),
            getAccountMeta('payer', accounts.payer),
            getAccountMeta('systemProgram', accounts.systemProgram),
            getAccountMeta('eventAuthority', accounts.eventAuthority),
            getAccountMeta('program', accounts.program),
        ],
        data: getCreateOperatorAccountInstructionDataEncoder().encode(
            args as CreateOperatorAccountInstructionDataArgs
        ),
        programAddress,
    } as CreateOperatorAccountInstruction<
        TProgramAddress,
        TAccountOperator,
        TAccountWhitelistedAddress,
        TAccountSigner,
        TAccountPayer,
        TAccountSystemProgram,
        TAccountEventAuthority,
        TAccountProgram
    >)
}

export type ParsedCreateOperatorAccountInstruction<
    TProgram extends string = typeof DYNAMIC_BONDING_CURVE_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>
    accounts: {
        operator: TAccountMetas[0]
        whitelistedAddress: TAccountMetas[1]
        signer: TAccountMetas[2]
        payer: TAccountMetas[3]
        systemProgram: TAccountMetas[4]
        eventAuthority: TAccountMetas[5]
        program: TAccountMetas[6]
    }
    data: CreateOperatorAccountInstructionData
}

export function parseCreateOperatorAccountInstruction<
    TProgram extends string,
    TAccountMetas extends readonly AccountMeta[],
>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>
): ParsedCreateOperatorAccountInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 7) {
        throw new SolanaError(
            SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
            {
                actualAccountMetas: instruction.accounts.length,
                expectedAccountMetas: 7,
            }
        )
    }
    let accountIndex = 0
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[
            accountIndex
        ]!
        accountIndex += 1
        return accountMeta
    }
    return {
        programAddress: instruction.programAddress,
        accounts: {
            operator: getNextAccount(),
            whitelistedAddress: getNextAccount(),
            signer: getNextAccount(),
            payer: getNextAccount(),
            systemProgram: getNextAccount(),
            eventAuthority: getNextAccount(),
            program: getNextAccount(),
        },
        data: getCreateOperatorAccountInstructionDataDecoder().decode(
            instruction.data
        ),
    }
}
